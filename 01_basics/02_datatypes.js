"use strict"; // treat all JS code as newer version

// alert( 3 + 3 ) // we are using node.js not browser

console.log(3 + 3);
console.log("Karan");


let name = "Karan" // String
let age = 18 // Integer
let isLoggedIn = false // Boolean

let state // undefined
let city = null; // standalone value

console.log(typeof city);  // typeof returns the type of the value